

<aside id="nav">
  <ul id="nav-link-container">
    <li id="nav-link"><a href="#">My links</a></li>
    <li id="nav-link"><a href="#">Popular links</a></li>
    <li id="nav-link"><a href="#">New links</a></li>
  </ul>
  </aside>

<aside id="news-feed">
</aside>

<section id="links-management">
    <form id="new-link" action='/links' method='post'>
      <input type='text' name='url' placeholder="Url">
      <input type='text' name='title' placeholder="Title">
      <input type='text' name='tags' placeholder="Tags">
      <input type='submit' value='Add link'>
    </form>

  <form id="filter" action='/tags/:text' method='get'>
    <input type='text' name='filter' placeholder="Filter current links"><input type='submit' value='Submit'>
  </form>
</section>

  <% if @links.any? %>
    <ul id='links-list'>
      <% @links.each do |link| %>

        <li id='link-container'>

          <section id="link">
            <div id="bookmark-ribbon">
              <a id="link-title" href="<% link.url %>"><%= link.title %></a>
            </div>
          </section>

          <section id='tags'>
            <% if link.tags.any? %>
              <% link.tags.each do |tag| %>
                  <a href="#" id='tag'><%= tag.text %></a>
              <% end %>
            <% end %>
          </section>

        </li> 
      <% end %>
    </ul>
  <% else %>
    <h4>Get out there and find some cool pictures of cats or something dude!</h4>

<% end %>
